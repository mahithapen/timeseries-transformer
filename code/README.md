# Code

Place all implementation code, configs, and scripts here.

Suggested structure (optional):
- `code/models/` PatchTST modules
- `code/train.py` training entry point
- `code/eval.py` evaluation entry point
- `code/configs/` experiment configs

Add a `requirements.txt` once dependencies are finalized.
